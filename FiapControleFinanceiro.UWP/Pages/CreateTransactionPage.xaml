<Page
    x:Class="FiapControleFinanceiro.UWP.Pages.CreateTransactionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FiapControleFinanceiro.UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cvt="using:FiapControleFinanceiro.UWP.Converters"
    xmlns:models="using:FiapControleFinanceiro.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <cvt:TypeToObjectConverter x:Key="TypeToObject"/>
        <cvt:ByteArrayToImageSourceConverter x:Key="ByteArrayToImageSource"/>
        <cvt:NullToBooleanConverter x:Key="NullToBoolean" />
        <cvt:NullToVisibilityConverter x:Key="NullToVisibility" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Padding="12">
            <StackPanel>
                <TextBox Header="Id" Text="{x:Bind ViewModel.Transaction.Id, Mode=TwoWay}" Margin="0,12"/>
                <TextBox Header="Data de Criação" Text="{x:Bind ViewModel.Transaction.CreatedDate, Mode=TwoWay}" AcceptsReturn="True" 
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         Height="200" Margin="0,12" />
                <TextBox Header="Data de Processamento" Text="{x:Bind ViewModel.Transaction.ProcessmentDate, Mode=TwoWay, Converter={StaticResource TypeToObject}}" Margin="0,12"/>
                <!--<ComboBox Header="Categoria" ItemsSource="{x:Bind ViewModel.Categorias}" SelectedItem="{x:Bind ViewModel.Receita.Categoria, Mode=TwoWay}">
                    <DataTemplate x:DataType="models:Categoria">
                        <StackPanel>
                            <TextBlock Text="{x:Bind }" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox>-->
                <TextBox Header="Preço" Text="{x:Bind ViewModel.Transaction.Id, Mode=TwoWay, Converter={StaticResource TypeToObject}}" Margin="0,12"/>
                <StackPanel Margin="0,12">
                    <!--<Image Height="200" Source="{x:Bind ViewModel.Receita.ImagemBytes, Mode=OneWay, Converter={StaticResource ByteArrayToImageSource}}"
                        Visibility="{x:Bind ViewModel.Receita.ImagemBytes, Mode=OneWay, Converter={StaticResource NullToVisibility}}" Margin="4"/>-->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!--<Button Click="{x:Bind ViewModel.CarregarImagem}" Margin="100,0">
                            <SymbolIcon Symbol="BrowsePhotos"/>
                        </Button>
                        <Button Click="{x:Bind ViewModel.ExcluirImagem}"
                             IsEnabled="{x:Bind ViewModel.Receita.ImagemBytes, Mode=OneWay, Converter={StaticResource NullToBoolean}}">
                            <SymbolIcon Symbol="Delete"/>
                        </Button>-->
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <CommandBar Grid.Row="1" Background="Transparent">
            <AppBarButton Label="Compartilhar" Icon="Share" Click="{x:Bind ViewModel.CompartilharTransacao}" />
            <AppBarButton Label="Excluir" Icon="Delete" Click="{x:Bind ViewModel.ExcluirTransacao}" />
        </CommandBar>
    </Grid>
</Page>
